1.数组默认值如果为函数，进行（惰性求值），只有（undifined）进行使用默认值时，才会调用函数
2.Math对象方法中，非数值先进行数值转换，再运算的的方法有：（Trunc）、（cbrt）、（hypot）和三角函数
3.如果变量解构不成功，变量的值就等于（undefined）。
4.模板字符串中嵌入变量，需要将变量名写在（${}）之中。
5.(Array.from)方法用于将两类对象转为真正的数组
6.数组结构的默认值：只有数组成员严格等于(undifined)时默认值才会生效，否则不生效
7、es6中，（）与JQ中的$.extend的效果是一样的。
8、let [head, ...tail] = [1, 2, 3, 4];以上这次赋值中tail的打印结果是（）
9、如果箭头函数不需要参数或需要多个参数，就使用一个（圆括号）代表参数部分
10、函数体内的this对象，就是定义时所在的对象，而不是（使用时所在的对象）
11.((...x, xs)=>x)(1,2,3)答案：error
12.类数组转数组 答案：Arry.from
13.Array.from()方法的作用（）
14.es6新增的数据结构（）
15.字符串模板用什么来输出变量（）
16.Object.is(NaN,NaN)运行结果是什么（）
17.变量的声明和赋值是（一体）的。必须（同时）进行。
18.字符串解构为数组：首先把字符串转换为（类数组）的形式进行解构；
19.如果模板字符串中需要使用反引号，必须是（字符串）的转义符
20.数组的空位指，数组的某一个位置没有（任何值）
21.Number.isNaN()用来(检查一个值是否为NaN)
22.Math.trunc(）
   与parseInt（）区别：
   （一）、如果无穷大，parseInt为NaN,trunc为Inifity；
   （二）、如果字符串中前为数字后为非数字， parseInt取整成功，trunc为NaN
   （三）、如果为布尔值、null、NaN、""，parseInt为NaN,trunc进行数值的转义；
   （四）、如果为undifined，都为NaN
    (五)、如果非数字，内部先将其转换为数值